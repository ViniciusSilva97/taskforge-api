[project]
name = "taskforge-api"
version = "0.1.0"
description = "API de gerenciamento de tarefas com fluxo de entrega e revisão."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.139.0",
]
