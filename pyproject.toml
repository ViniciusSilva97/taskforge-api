[project]
name = "taskforge-api"
version = "0.4.0"
description = "API de tarefas com autenticação JWT, paginação e notificações gerenciáveis."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "alembic>=1.18.0",
    "argon2-cffi>=25.1.0",
    "fastapi[standard]>=0.139.0",
    "psycopg[binary]>=3.2.0",
    "pyjwt>=2.13.0",
    "sqlalchemy>=2.0.0",
]
